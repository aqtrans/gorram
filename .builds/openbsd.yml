image: openbsd/7.3
packages:
  - go
  - git
secrets:
  - 1efbd7f2-d0b5-4cfc-8a0c-f1c12da95d4e
sources:
  - ssh://git@git.squanch.space/~aqtrans/gorram
tasks:
  - setup: |
      cd gorram
      go get -d
      go install github.com/twitchtv/twirp/protoc-gen-twirp
      go install google.golang.org/protobuf/cmd/protoc-gen-go
      go generate
  - test: |
      cd gorram
      go test
      go test -race
      go test -cover
      go test -bench=.
  - build: |
      cd gorram
      go build -ldflags "-X main.sha1ver=$(git rev-parse HEAD) -X main.buildTime=$(date +'%Y-%m-%d_%T')" -o gorram-client ./client
      go build -ldflags "-X main.sha1ver=$(git rev-parse HEAD) -X main.buildTime=$(date +'%Y-%m-%d_%T')" -o gorram-server ./server
      go build -o gorram-cli ./cli
#  - deploy: |
#      cd gorram
#      scp -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no gorram-server deploy@morty.squanch.space:/tmp/gorram-server
#      ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no deploy@morty.squanch.space "doas /bin/mv /tmp/esg /usr/local/bin/gorram-server"
#      ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no deploy@morty.squanch.space "doas /usr/sbin/rcctl restart gorram-server"
#      scp -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no gorram-client deploy@morty.squanch.space:/tmp/gorram-client
#      ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no deploy@morty.squanch.space "doas /bin/mv /tmp/gorram-client /usr/local/bin/gorram-client"
#      ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no deploy@morty.squanch.space "doas /usr/sbin/rcctl restart gorram-client"
